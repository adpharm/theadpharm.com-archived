---
import Href from "@/components/Href";

interface Props {
  id: string;
  className?: string;
}

const { id, className } = Astro.props;
---

<div id={id} class={`grid lg:grid-cols-5 ${className}`}>
  <div class="lg:col-span-2">
    <slot name="left" />
  </div>
  <div class="lg:col-span-3">
    <div class="w-full">
      <slot name="right" />
    </div>
  </div>
</div>
